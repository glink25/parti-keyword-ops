{
  "name": "parti-keyword-ops",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify": "npm run typecheck && npm run test && npm run build && node scripts/verify-build.mjs",
    "local-build": "node scripts/local-build.mjs",
    "local-verify": "npm run local-build && node scripts/rules-smoke.mjs && node scripts/worker-integration.mjs && node scripts/verify-build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
